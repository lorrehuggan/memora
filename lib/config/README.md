# Environment Configuration

Environment variables are configured via app.config.js and accessed via lib/config/env.ts

Usage: import { API_URL, BASE_URL, BETTER_AUTH_SECRET } from "@/lib/config/env"

Remember to restart Expo dev server after changing .env file
